# NanoGPT
Building a NanoGPT-style transformer language model from scratch in Python and PyTorch to understand the complete LLM pipeline. The project covers text preprocessing, tokenization, embeddings, positional embeddings, self-attention, transformer blocks, model training, and next-token generation without relying on high-level transformer libraries.
